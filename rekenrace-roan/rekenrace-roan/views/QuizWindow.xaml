<Window x:Class="rekenrace_roan.views.QuizWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:rekenrace_roan.views"
        xmlns:viewmodels="clr-namespace:rekenrace_roan.ViewModels"
        Title="Rekenrace Quiz"
        Height="450"
        Width="800">
    <Window.Resources>
        <Style x:Key="HeaderLabelStyle"
               TargetType="Label">
            <Setter Property="VerticalAlignment"
                    Value="Center" />
            <Setter Property="FontSize"
                    Value="24" />
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="Margin"
                    Value="10" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <!-- Top Header with Background -->
        <Border Grid.Row="0"
                Background="LightBlue">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <Label Content="{Binding Player.Name}"
                       Style="{StaticResource HeaderLabelStyle}"
                       Grid.Column="0"
                       HorizontalAlignment="Left" />
                <Label Content="{Binding Player.Difficulty}"
                       Style="{StaticResource HeaderLabelStyle}"
                       Grid.Column="1"
                       HorizontalAlignment="Right" />
                <Button Content="Terug"
                        Grid.Column="2"
                        Margin="10"
                        Click="BackToMainMenu_Click" />
            </Grid>
        </Border>

        <!-- Quiz Content -->
        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <TextBlock Text="{Binding CurrentProblem.Equation}"
                       FontSize="36"
                       Margin="10" />
            <TextBlock Text="{Binding CurrentProblemNumber, StringFormat=Vraag {0} van {1}}"
                       HorizontalAlignment="Center"
                       Margin="10" />
            <TextBox x:Name="txtAnswer"
                     Width="200"
                     Height="40"
                     FontSize="24"
                     Text="{Binding UserAnswer, UpdateSourceTrigger=PropertyChanged}"
                     PreviewTextInput="txtAnswer_PreviewTextInput" />
            <Button Content="Controleer"
                    Width="200"
                    Height="40"
                    Margin="10"
                    Command="{Binding CheckAnswerCommand}"
                    Click="CheckAnswer_Click" />
        </StackPanel>

        <!-- Navigation -->
        <Button Content="Volgende"
                Grid.Row="2"
                Width="200"
                Height="40"
                Margin="10"
                Command="{Binding NextProblemCommand}"
                Click="NextProblem_Click" />
    </Grid>
</Window>